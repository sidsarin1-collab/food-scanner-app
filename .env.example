# Postgres connection string, e.g. postgresql://user:password@localhost:5432/food_scanner
DATABASE_URL=

# Shared password for the /admin route (v1: single shared password, no user accounts)
ADMIN_PASSWORD=
